namespace {{namespace}}.Generated;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on
// next export.
//
// [Round 96] Marker interface for "this card belongs to this mod, swap its
// star-cost badge texture for the custom star icon." Confirmed real via a
// genuinely new evidence source this round: `TheBurdenedNewCharacter`'s own
// decompiled source turned out to be Forge-generated itself (every one of
// its files carries the literal header "AUTO-GENERATED by STS2 Character
// Creator" — a more advanced build of this same tool, not a hand-authored
// mod), so its `Patches/TexturePatches.cs` is a real, working, in-game-
// proven example of exactly this feature, not a guess. Every one of that
// mod's generated card classes implements this exact empty marker
// (`global::TheBurdenedNewCharacter.Patches.IStarIconCard`); Forge now does
// the same, on every generated card unconditionally (see Card.cs.template) —
// harmless on a card that never shows a star badge, since the Harmony
// postfix that reads this marker only fires from NCard.UpdateStarCostVisuals,
// which the game itself only calls for star-cost cards.
//
// See Generated/ForgeStarIconActivation.cs for the two Harmony patches that
// actually consume this marker.
public interface IStarIconCard
{
}

using BaseLib.Abstracts;
using System.Collections.Generic;
using MegaCrit.Sts2.Core.Models;

namespace {{namespace}}.PotionPools;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on next export.
//
// UPDATED after running tools/reflect-baselib against the real installed
// sts2.dll + BaseLib.dll — see TOOLCHAIN_FINDINGS.md "reflect-baselib round
// 1". This also CONFIRMED the base class name guessed in the previous
// version of this file (`CustomPotionPoolModel`) is real. PotionPoolModel's
// abstract surface is exactly these three members. character.schema.json
// has no potion concept yet, so GenerateAllPotions() always returns empty —
// nothing populates this pool; it exists purely to satisfy the override.
public sealed class {{className}} : CustomPotionPoolModel
{
    // [UNVERIFIED — cosmetic only, any string compiles]
    public override string EnergyColorName => "";

    // `protected` (NOT `public`) — CONFIRMED by a real build (CS0507); the
    // base `PotionPoolModel.GenerateAllPotions()` is declared `protected`.
    protected override IEnumerable<PotionModel> GenerateAllPotions() => new List<PotionModel>();

    // [VERIFIED to be required via reflect-baselib; value guessed]
    public override bool ShouldReceiveCombatHooks => false;
}

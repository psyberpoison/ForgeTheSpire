using MegaCrit.Sts2.Core.Timeline;

namespace {{namespace}}.Timeline;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on
// next export.
//
// [VERIFIED via direct ECMA-335 IL disassembly of the real installed
// sts2.dll's MegaCrit.Sts2.Core.Timeline.StoryModel AND of Tyler's own
// updated TheBurdenedNewCharacter.dll (v3, "New Chronicle" update) — see
// claude/round203-chronicles-epochs.md] StoryModel is a Chronicle in
// Tyler's terms — a real base-game class, registered the same
// reflection-based way as EpochModel (see Timeline/ChronicleRegistrar.cs).
// Id/Epochs are abstract (must override). Id is used ONLY as this
// Chronicle's own registry key here — every {{namespace}} epoch's own
// StoryId override (see Epoch.cs.template) is generated from this exact
// same literal, so the two always agree (the reference mod's own compiled
// output has a real casing mismatch between an epoch's StoryId and its
// story's own Id — Forge avoids that bug by generating both from one
// value instead of composing them independently).
public class {{className}} : StoryModel
{
    protected override string Id => "{{storyId}}";

    public override EpochModel[] Epochs => new EpochModel[]
    {
{{epochInstances}}
    };
}

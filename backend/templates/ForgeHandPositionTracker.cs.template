using HarmonyLib;
using MegaCrit.Sts2.Core.Models;

namespace {{namespace}}.Generated;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on
// next export.
//
// [VERIFIED via decompiling TheBurdenedNewCharacter.dll v4's
// ModHandPositionPatch — see Generated/IForgeHandPosition.cs for the full
// evidence trail] Same real Harmony-prefix technique Tyler's own updated
// mod uses to capture "was this card leftmost/rightmost when played",
// reimplemented generically here (index+count instead of two fixed
// booleans) so any Forge card's CardPositionInHand condition (on its own
// OnPlay, or another card's/relic's OnAnyCardPlayed reaction via
// cardPlay.Card) can check ANY edge/position, not just leftmost/rightmost.
//
// [Round 56] The actual per-card capture logic now lives in
// ForgeActions.CaptureHandPositionBeforeRemoval — shared with
// Generated/ForgeHandPositionDiscardTracker.cs and
// Generated/ForgeHandPositionTurnEndTracker.cs, the two new capture points
// added this round (see IForgeHandPosition.cs's header for why OnDiscard/
// OnTurnEndInHand needed the exact same "before removal" trick OnPlay
// does). This class now only does the OnPlayWrapper-specific part: being
// the Prefix, and passing `__instance` through.
//
// ModEntry.cs already bootstraps `new Harmony(...).PatchAll()`, which
// picks up this [HarmonyPatch] class automatically — no extra wiring
// needed, same as ForgeCardTrailNullGuard/ForgeCreatureVisualsNullGuard.
//
// Prefix (not Postfix) is what makes this real: it runs before
// OnPlayWrapper's own body, which is BEFORE the card is removed from the
// Hand pile — see IForgeHandPosition.cs's comment for the confirmed IL
// call order.
[HarmonyPatch(typeof(CardModel), nameof(CardModel.OnPlayWrapper))]
public static class ForgeHandPositionTracker
{
    static void Prefix(CardModel __instance)
    {
        ForgeActions.CaptureHandPositionBeforeRemoval(__instance);
    }
}

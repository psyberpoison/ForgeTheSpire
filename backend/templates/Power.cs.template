using System.Collections.Generic;
using System.Threading.Tasks;
using System.Linq; // [Round 199] added so ClearAfflictionFromPile's real .Where(...).ToList() pile-scan compiles here too -- see compiler.js's own comment on that action case.
using BaseLib.Abstracts;
using MegaCrit.Sts2.Core.Combat;
using MegaCrit.Sts2.Core.Commands.Builders;
using MegaCrit.Sts2.Core.Entities.CardRewardAlternatives;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.Entities.Creatures;
using MegaCrit.Sts2.Core.Entities.Merchant;
using MegaCrit.Sts2.Core.Entities.Players;
using MegaCrit.Sts2.Core.Entities.Powers;
using MegaCrit.Sts2.Core.Entities.RestSite;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Localization;
using MegaCrit.Sts2.Core.Map;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.Rewards;
using MegaCrit.Sts2.Core.Rooms;
using MegaCrit.Sts2.Core.Runs;
using MegaCrit.Sts2.Core.ValueProps;
using {{namespace}}.Generated;

// Round 20 (2026-08-25): added 9 `using`s (Commands.Builders,
// Entities.CardRewardAlternatives, Entities.Merchant, Entities.Players,
// Entities.RestSite, Localization, Map, Rewards, Rooms) — same fix, same
// reasoning, as Relic.cs.template's own header comment. Several of Round
// 19's 39 new triggers reference bare type names (`Player`, `AttackCommand`,
// `CombatRoom`, `MerchantEntry`) whose real namespaces weren't imported
// here either.

namespace {{namespace}}.Powers;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on
// next export.
//
// REWRITTEN after Tyler's stress-test build attempt #5 caught a real
// CS0246 ("The type or namespace name 'CustomPowerModel' could not be
// found") — this file was missing `using BaseLib.Abstracts;` (that's
// where CustomPowerModel actually lives — every other generated file
// already had this import, this one was just never build-tested before
// since Tyler's earlier test characters had 0 mechanics). Also caught in
// the same stress-test round: mechanics had NO way to define what they
// actually DO on trigger — the class only ever had the three required
// PowerType/PowerStackType/ShouldReceiveCombatHooks overrides, nothing
// else. `mechanic.effects` (schema, new) + the hook-method block generated
// below close that gap, reusing the exact same trigger -> real hook
// method infrastructure already built for relics
// (compiler.js:TRIGGER_HOOKS / generateHookEffects — see
// Relic.cs.template and TOOLCHAIN_FINDINGS.md "reflect-baselib round 2").
//
// PowerModel/CustomPowerModel's real abstract surface [VERIFIED by
// reflection, reflect-baselib round 1]:
//   PowerType Type
//   PowerStackType StackType
//   bool ShouldReceiveCombatHooks
// PowerType is None/Buff/Debuff, PowerStackType is None/Counter/Single
// [VERIFIED enum members, round 2] — the MAPPING from this mechanic's
// isBuff/stackable schema fields onto those specific members is a
// reasonable guess (compiler.js:generateMechanicSource), not confirmed
// against real in-game behavior.
//
// The hook-method block generated below uses the SAME trigger -> real
// BaseLib hook method mapping as Relic.cs.template (both CustomPowerModel
// and CustomRelicModel derive AbstractModel, which is where these hook
// overrides actually live) — 'OnPlay' is deliberately NOT a valid
// trigger here, same reasoning as relics: a power isn't "played", it's
// applied to a creature and then reacts to combat events. One honest gap
// worth flagging: that block's player/target binding per trigger
// (e.g. OnTakeDamage -> target/dealer) is the same best-effort binding
// used for relics — NONE of it has been confirmed to specifically be
// "the creature that HOLDS this power instance" (an Owner/Holder-style
// property on PowerModel, if one exists, hasn't been searched for by
// reflect-baselib yet — a good round 9 candidate). For triggers where the
// binding happens to line up anyway (e.g. a poison-style "this power's
// owner takes damage" via OnTakeDamage), it's usable as-is; for anything
// that needs "my own owner" specifically regardless of trigger, treat
// `fgPlayer`/`fgTarget` here with the same care as everywhere else
// tagged [BEST EFFORT] in this project.
public sealed class {{className}} : CustomPowerModel
{
    public override PowerType Type => PowerType.{{powerTypeGuess}}; // [VERIFIED member; mapping guessed]
    public override PowerStackType StackType => PowerStackType.{{powerStackTypeGuess}}; // [VERIFIED member; mapping guessed]
    public override bool ShouldReceiveCombatHooks => true; // [VERIFIED to be required; value guessed]
{{visibilityOverride}}

{{hookMethods}}
}

using System.Collections.Generic;
using System.Threading.Tasks;
using System.Linq; // [Round 199] added so ClearAfflictionFromPile's real .Where(...).ToList() pile-scan compiles here too -- see compiler.js's own comment on that action case.
using BaseLib.Utils;
using BaseLib.Abstracts;
using MegaCrit.Sts2.Core.Combat;
using MegaCrit.Sts2.Core.Commands.Builders;
using MegaCrit.Sts2.Core.Entities.CardRewardAlternatives;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.Entities.Creatures;
using MegaCrit.Sts2.Core.Entities.Merchant;
using MegaCrit.Sts2.Core.Entities.Players;
using MegaCrit.Sts2.Core.Entities.Relics;
using MegaCrit.Sts2.Core.Entities.RestSite;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Localization;
using MegaCrit.Sts2.Core.Map;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Rewards;
using MegaCrit.Sts2.Core.Rooms;
using MegaCrit.Sts2.Core.Runs;
using MegaCrit.Sts2.Core.ValueProps;
using {{namespace}}.RelicPools;
using {{namespace}}.Generated;
using {{namespace}}.Powers;

// This exact using list (minus BaseLib, BaseLib.Abstracts, and the
// Generated/RelicPools imports) is copied from the one [VERIFIED] real
// relic file we found —
// https://github.com/lamali292/sts2_example_mod/blob/main/Relics/ExampleRelic.cs
// — since it's the only confirmed-working set of imports for
// PlayerChoiceContext/CardModel/DamageResult/ValueProp/IRunState.
//
// `using BaseLib.Utils;` and `using {{namespace}}.RelicPools;` added after
// real build attempts #2 and #4 hit the identical [Pool(...)]/pool-namespace
// errors on Card.cs.template — `BaseLib.Utils.PoolAttribute` is now
// CONFIRMED via reflect-baselib round 4. `System.Collections.Generic` (IEnumerable/
// IReadOnlyList), `MegaCrit.Sts2.Core.Combat` (CombatSide/ICombatState),
// and `MegaCrit.Sts2.Core.Entities.Cards` (CardPlay) added after
// reflect-baselib round 2 confirmed the REAL hook method signatures use
// these types — see TOOLCHAIN_FINDINGS.md "reflect-baselib round 2" and
// compiler.js's TRIGGER_HOOKS.
//
// Round 20 (2026-08-25) added 9 more `using`s — Commands.Builders,
// Entities.CardRewardAlternatives, Entities.Merchant, Entities.Players,
// Entities.RestSite, Localization, Map, Rewards, Rooms. Found via a fresh
// full ECMA-335 read of every real AbstractModel hook's parameter types
// (see sts2-builder/tools/sts2tools/abstractmodel_full_dump.txt) — several
// of Round 19's own 39 new triggers reference bare type names (`Player`,
// `AttackCommand`, `CombatRoom`, `MerchantEntry`) whose real namespaces
// were NOT in this file's using list before this fix, which would have
// been a real CS0246 on first compile. Caught and fixed here BEFORE
// Tyler's first Round 19 build attempt finished, not after — see
// claude/round20-groupb-findings.md.

namespace {{namespace}}.Relics;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on next export.
//
// BEST EFFORT: modeled on the [VERIFIED] real relic
// (https://github.com/lamali292/sts2_example_mod/blob/main/Relics/ExampleRelic.cs),
// which extends the plain MegaCrit RelicModel rather than BaseLib's
// CustomRelicModel. We use CustomRelicModel here instead because the
// [Pool(typeof(...))] attribute is how a relic gets tied to ONE specific
// custom character's relic pool (confirmed to exist in BaseLib by name,
// per the decompiled mods) rather than being globally available — that's
// what a per-character relic needs. The hook method signatures below
// (AfterCardPlayed etc.) follow the verified relic's shape; which of the
// eleven trigger names in character.schema.json map onto which real BaseLib
// override is still a best-effort guess per trigger — see the switch below
// and TOOLCHAIN_FINDINGS.md.
//
// `Id`/`Name` overrides REMOVED here proactively based on the same CS0506
// evidence that killed them on Card and Character — see Card.cs.template's
// header for the full reasoning. Notably, the one [VERIFIED] real relic
// above never overrides Id or Name either.
//
// [VERIFIED via reflect-baselib round 1] RelicModel/CustomRelicModel's ONLY
// ABSTRACT members are exactly `Rarity` and `ShouldReceiveCombatHooks` —
// both already present below, unchanged.
//
// [VERIFIED via reflect-baselib round 2] every hook method name AND its
// real full parameter list — round 2 dumped every concrete/virtual
// After*/On*/Before* member across CustomRelicModel's whole hierarchy
// directly from real sts2.dll/BaseLib.dll. See compiler.js's TRIGGER_HOOKS
// for the per-trigger mapping (now the real signatures, not a guess) and
// TOOLCHAIN_FINDINGS.md "reflect-baselib round 2" for the full dump. A
// couple of schema triggers (OnCombatStart, OnKillEnemy) don't have an
// exact real analog and are remapped to the closest real method
// (BeforeCombatStart, AfterDeath respectively) — see TRIGGER_HOOKS'
// comments for the caveats on each.
[Pool(typeof({{poolClassName}}))]
public sealed class {{className}} : CustomRelicModel
{
    public override RelicRarity Rarity => RelicRarity.{{rarity}};
    public override bool ShouldReceiveCombatHooks => true;
{{iconOverride}}

{{hookMethods}}
}

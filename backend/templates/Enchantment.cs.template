using System;
using System.Threading.Tasks;
using BaseLib.Utils;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.Entities.Creatures;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.ValueProps;
using {{namespace}}.Generated;

namespace {{namespace}}.Enchantments;

// AUTO-GENERATED by Forge -- do not hand-edit, changes will be overwritten on next export.
//
// [VERIFIED via direct ECMA-335 metadata read of the real installed
// sts2.dll, round 190 -- tools/sts2tools/ecma_dump_ext.py against
// MegaCrit.Sts2.Core.Models.EnchantmentModel] this is a REAL base-game
// class, not a guess -- real concrete enchantments Inky and Sown both
// override the exact same OnPlay(PlayerChoiceContext, CardPlay) signature
// used below, proving it's a live shipped hook, not dead/abstract-only
// code. This overturns this project's long-standing "still unresearched"
// caveat about enchantments (see claude/round169-enchantments-rough-pass.md,
// claude/feature-backlog.md group C) -- see claude/round190-enchantments-
// real-codegen.md for the full reflection dump this template is built from.
//
// No [Pool(typeof(...))] attribute here, unlike Card/Relic/Power -- the
// reflected metadata has no CustomXPoolModel-shaped container for
// enchantments, and ModelDb.Enchantment<T>() (the confirmed real generic,
// same shape as ModelDb.Card<T>()/ModelDb.Relic<T>()) doesn't take one
// either. EnchantmentModel is presumed [BEST EFFORT] to be discovered by
// the same generic AbstractModel-subclass reflection scan Card.cs.template's
// own header describes for ModelDb.Init_Patch9 (Pool ties a class to a
// per-character POOL for selection UI; it isn't what makes a class
// discoverable in the first place) -- not separately confirmed this round.
//
// IMPORTANT KNOWN GAP, please read before relying on this: this class's
// logic is real and will run once something in-game actually attaches an
// instance of it to a CardModel -- but Forge has NO action or mechanism
// yet (no card effect, no relic effect, no event) that GRANTS this
// enchantment to a card at runtime. Until a future round adds one (e.g. a
// "GrantEnchantment" card/relic action referencing
// ModelDb.Enchantment<{{className}}>(), the same reference-expression
// pattern actionToCSharp's CreateCard case already uses for
// ModelDb.Card<T>()), this class compiles and is logically correct but is
// never actually instantiated by your mod. See this enchantment's own
// entry in Enchantments/README.md for the plain-language version of this
// caveat.
//
// Field-mapping legend used throughout this file's generator
// (compiler.js:generateEnchantmentSource): [VERIFIED] = confirmed real
// override with a confirmed signature, [BEST EFFORT] = confirmed real
// override where the exact runtime SEMANTICS (e.g. what "multiplicative"
// scales against) are Forge's own reasonable reading, not decompiled from
// a real example, and [UNVERIFIED] = left uncompiled (a plain comment, or
// a ForgeActions.Todo(...) stub) because no BaseLib API for it was
// confirmed this round.
//
// Note on the three gating/freeform fields (onceOnlyPerCombat,
// firstPlayOnly, randomizeEnergyOnDraw) specifically: these are left as
// COMMENTS, not ForgeActions.Todo() calls, on purpose. Todo() throws the
// instant its line actually runs (see compiler.js's own round-30
// postmortem, where a reached Todo() call froze an entire real combat
// turn) -- and these three would sit in front of or alongside every OTHER
// effect below. Throwing there would break this enchantment's CONFIRMED
// effects too, every single time it's played, not just the unconfirmed
// part. A silent no-op comment is the more honest failure mode until a
// real API is found for them. "Extra effects (freeform)" text IS compiled
// as a trailing Todo() (matching actionToCSharp's own convention for
// genuinely freeform/unrecognized content) since it's placed LAST, after
// every confirmed per-stack effect already ran.
public sealed class {{className}} : EnchantmentModel
{
{{modifierOverrides}}
{{onEnchantMethod}}
    // [VERIFIED signature] OnPlay(PlayerChoiceContext, CardPlay) -- the
    // same real hook Inky/Sown both override; cardPlay.Player/.Card follow
    // the identical confirmed shape Card.cs.template's own OnPlay body
    // already uses (see compiler.js's generateCardSource).
    public override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        var fgPlayer = cardPlay.Player.Creature;
        var enchantStacks = {{perStackFlag}} ? this.Amount : 1; // [BEST EFFORT] Amount is a real, non-virtual int property (get_Amount/set_Amount) -- "Per Stack multiplies by it" is Forge's own chosen semantic, not something BaseLib itself defines
{{onPlayBody}}
        await Task.CompletedTask;
    }
}

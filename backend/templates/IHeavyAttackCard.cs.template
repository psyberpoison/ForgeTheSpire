namespace {{namespace}}.Generated;

// AUTO-GENERATED by Forge — do not hand-edit, changes will be overwritten on
// next export.
//
// [Round 121] Marker interface for "when this card is played, play the
// character's heavyAttack pose instead of its plain attack pose" — see
// character.poseSheets in schema/character.schema.json for the full
// mechanism. Mirrors IStarIconCard.cs's own established shape exactly (an
// empty marker interface, applied only to the specific cards that opt in
// via card.advancedOptions.heavyAttackAnimation — see Card.cs.template's
// extraInterfaces splice), read by the generated character class's own
// AfterCardPlayed override (the generated character class) via a plain
// `cardPlay.Card is IHeavyAttackCard` check — no Harmony patch needed here,
// unlike IStarIconCard's activation, since the character class itself is
// Forge-generated code that can check this directly.
public interface IHeavyAttackCard
{
}
